const project=window.projectData.find(p=>p.id===new URLSearchParams(location.search).get('id'));
if(project){document.title=`${project.name} — ONEWORK`;document.querySelector('h1').textContent=project.name;document.querySelector('.detail-category').textContent=project.label;document.querySelector('.detail-copy').textContent=project.description;project.images.forEach((src,i)=>{const img=new Image();img.src=src;img.alt=`${project.name} — ${['spatial model','structure detail','connected system','performance insight'][i]}`;document.querySelector('.detail-gallery').append(img);});}else{document.querySelector('h1').textContent='Project not found';document.querySelector('.detail-copy').textContent='Return to the project gallery to explore the available concept studies.';}
